## [2026-09-03 23:38] Session summary

**Done this session:**
- Built high-precision vacancy classifier (`src/scraper/classifier.py`):
  - Added multi-signal classification with comprehensive disqualifying filters:
    - Charity / medical / donation appeals ("helping someone in need", "gofundme", "kidney transplant", "እርዳታ").
    - CV/Resume writing and redesign services selling to job seekers.
    - Course/bootcamp/training programs selling education ("registration fee", "masterclass").
    - Job seekers advertising themselves ("I am looking for a job", "ስራ ፈላጊ ነኝ").
    - Crypto airdrops, forex VIP signals, and earning scams.
    - Real estate / product sales / channel cross-promotion.
    - Short messages (<70 characters).
  - Multi-dimension positive scoring: Header/Title markers, Requirements/Qualifications markers, Application methods/Deadlines, and Terms/Salary.
  - Work modality detection: extracts `Remote`, `Hybrid`, `On-site`, or `Unspecified`.
- Upgraded Scraper (`src/scraper/runner.py`) and Database (`src/db/jobs.py`):
  - Scraper now filters through `is_certified_job_post` and extracts `work_type`.
  - Added `migrations/002_add_work_type_and_profile_storage.sql` and graceful fallback in `add_job_listing`.
  - Display modality badges (`🌐 Remote`, `🔄 Hybrid`, `🏢 On-site`) in Telegraph Instant View (`src/bot/formatters.py`) and Mini App job cards (`api/static/app.html`).
- Built Feature 1: User Profile & CV Upload (AGENTS.md highest priority):
  - Created Supabase Storage bucket `resumes` for cloud storage of uploaded CVs.
  - Built `src/db/profiles.py` with `get_user_profile`, `upsert_user_profile`, `upload_user_cv`, `get_user_cv_signed_url`, and `validate_cv_file` (max 10MB, `.pdf`, `.docx`, `.doc`).
  - Added CV Document handler in `src/bot/handlers.py` supporting direct PDF/DOCX file attachments and swaps.
  - Added `/profile`, `/skills <text>`, and `/experience <text>` commands in `src/bot/handlers.py`.
  - Added `/api/profile` (GET & POST) in `api/webhook.py`.
  - Updated Mini App Profile tab in `api/static/app.html` to fetch and render live CV status, document download link, skills, and experience.
  - Updated `scripts/setup_bot_profile.py` with new commands and synced them with Telegram Bot API.
- 31/31 unit tests passing (`tests/test_classifier.py`, `tests/test_profiles.py`, `tests/test_webhook.py`, `tests/test_formatters.py`, `tests/test_matching.py`, `tests/test_db.py`, `tests/test_scraper.py`).

***Done this chat:***
- High-precision vacancy classifier with expanded negative patterns (charity, CV services, courses, job seekers, crypto scams)
- Work modality classification (Remote / Hybrid / On-site) and badges in Instant View + Mini App
- User Profile feature with CV upload to Supabase Storage, skills & experience text editing, and bot commands
- 31/31 tests passing

***Decisions made:***
- CV files are validated for allowed formats (`.pdf`, `.docx`, `.doc`) and capped at 10MB, stored in Supabase Storage under `cvs/{telegram_id}_{filename}` with overwrite enabled (CV swap capability).
- Non-job posts are eliminated by strict multi-signal classifier requiring either (Header + Application method), (Header + Requirements), or at least 3 distinct vacancy dimensions, with zero negative disqualifiers.

**Open questions / blockers:**
- None. Everything is tested and working end-to-end.

**Next steps:**
- Feature 2: Application tracking by link (user sends a job link -> creates tracked application row using profile info).
- Feature 3: CV swap UI enhancements.
- Feature 4: Suggest-a-job-by-link with admin review.

**State of the repo:**
- 31/31 unit tests passing. Classifier, work modality, and User Profile with CV upload and cloud storage are fully operational.

## [2026-09-03 23:16] Session summary

**Done this session:**
- Overhauled Telegram Mini App (`api/static/app.html`) into a complete 3-tab experience with Apple Design principles:
  - 💼 **Jobs Feed** tab: live matched job cards fetched via `/api/jobs`, category filter chips, expandable summaries, date stamps, and direct "View Post ↗" links. 
  - 🎯 **Preferences** tab: sector navigation (Tech, Finance, Creative, Sales, Ops, Services), spring-animated category chips with haptics, live search, floating glass save bar with counter.
  - 👤 **Profile** tab: user avatar, name, ID, active categories count, alert schedule, and CV upload preview placeholder for next phase.
  - Frosted glass segmented bottom tab bar with spring scaling, adaptive dark/light theming from Telegram theme params.
- Upgraded Telegraph Instant View formatting (`src/bot/formatters.py`):
  - Magazine-style bulletin layout with date header, category headers with counts, blockquote job cards with role title, source badge, summary body, and apply links.
  - Capped at 40 jobs total / 10 per category to prevent `CONTENT_TOO_BIG` errors.
  - Empty state message when no vacancies match.
- Added on-demand `/jobs` command and `⚡ Latest Matches` button (`src/bot/handlers.py`):
  - Queries Supabase for recent jobs matching user preferences.
  - Generates Telegraph Instant View bulletin on the fly.
  - Returns inline buttons for Instant View and Mini App.
- Upgraded notifier (`src/notifier/runner.py`) to use watermark-based tracking instead of deleting all jobs after each run. Jobs persist in Supabase for Mini App browsing and on-demand `/jobs`.
- Enhanced `src/db/jobs.py` with `get_recent_job_listings`, `get_unnotified_job_listings`, and `get_matched_jobs_for_user` queries.
- Added `/api/jobs` endpoint in `api/webhook.py` for the Mini App Jobs Feed tab.
- Created `scripts/setup_bot_profile.py` and ran it successfully:
  - Set bot commands menu (`/start`, `/jobs`, `/preferences`, `/help`).
  - Set bot description and short bio.
  - Set native Telegram chat menu button to open the Mini App directly.
- 17/17 tests passing including new tests for `/api/jobs` endpoint and updated Mini App assertions.

***Done this chat:***
- Complete 3-tab Apple Design Mini App (Jobs Feed + Preferences + Profile)
- Telegraph Instant View magazine-style bulletin formatting
- On-demand `/jobs` command and inline button
- Watermark-based notifier (jobs persist for browsing)
- Bot profile, commands, description, and native Mini App menu button
- 17/17 tests passing

***Decisions made:***
- Jobs no longer deleted after notification runs. Instead, notifier uses a watermark (`__notifier_watermark__` in `scraper_state`) to track which jobs have already been dispatched, so jobs remain in Supabase for the Mini App feed and on-demand `/jobs` queries.
- Native Telegram chat menu button (`setChatMenuButton`) opens the Mini App directly instead of showing the standard commands menu.

**Open questions / blockers:**
- None. All changes are tested and ready to push.

**Next steps:**
- Push changes to GitHub and verify Vercel redeploy.
- Feature 1: User profile CV upload to Supabase Storage.
- Feature 2: Application tracking by link.
- Feature 3: CV swap.
- Feature 4: Suggest-a-job by link with admin review.

**State of the repo:**
- Full 3-tab Mini App, Instant View bulletin formatting, on-demand `/jobs`, and bot profile configuration are complete. 17/17 tests passing. Ready to push.
